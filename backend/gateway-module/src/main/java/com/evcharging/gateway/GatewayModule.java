package com.evcharging.gateway;

/**
 * Gateway Module — Spring Cloud Gateway for API routing and JWT authentication.
 *
 * <p>Handles routing to backend module controllers, JWT token validation, rate limiting, and CORS
 * configuration.
 */
public interface GatewayModule {}
