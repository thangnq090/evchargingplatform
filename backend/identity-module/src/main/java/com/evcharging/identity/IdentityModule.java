package com.evcharging.identity;

/**
 * Identity & Access Module — Module descriptor for Spring Modulith.
 *
 * <p>Handles authentication, authorization, user management, and RBAC. Module boundaries and
 * dependencies are enforced by Spring Modulith at build time.
 */
public interface IdentityModule {}
